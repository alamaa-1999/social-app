{
  "lexicon": 1,
  "id": "com.atproto.temp.checkStriker",
  "defs": {
    "main": {
      "type": "query",
      "description": "Checks whether a given account currently has the Striker role. Does not require auth; implemented by PDS. Role is not sensitive - com.atproto.temp.listStrikers already publicly enumerates every Striker DID. THIS IS A TEMPORARY UNSPECCED ROUTE, specific to this fork's role model.",
      "parameters": {
        "type": "params",
        "required": ["did"],
        "properties": {
          "did": { "type": "string", "format": "did" }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["isStriker"],
          "properties": {
            "isStriker": { "type": "boolean" }
          }
        }
      }
    }
  }
}
